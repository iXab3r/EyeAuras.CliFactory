# Shared RANDOM.ORG contract

This package contains the two actual shared command declarations, DTOs, validation and fixed
live-proof inventory/orchestration. HTTP calls remain in random-rest; browser selectors remain
in random-pw. It is not a universal service adapter or an alternate transport.
