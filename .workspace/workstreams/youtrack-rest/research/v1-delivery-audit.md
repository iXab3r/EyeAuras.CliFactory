# V1 delivery audit — final milestone preparation

Issue #6 owns acceptance. This audit does not close its checkboxes or equate endpoint count with release.
Current direct technical acceptance:118/118 REST plus download1/1; AR118/final functional verdict pending.

| Gate | Concrete remaining condition | Status / owner |
|---|---|---|
| Final functionality |18 exact rows in block-118.json plus separate issue download; correct help/CLI/public docs and no unsupported optional-field promise |Pending domain owners and root/reviewer |
| Deterministic coverage |Every final method/path/body/projection/empty/error case, Update denial before fetch, multipart permission-before-file access, download two-request/path/origin/redirect/size/cleanup/redaction and profile isolation |Pending focused tests; full final npm test required |
| Auth contract |Resolve or explicitly reconcile existing Core configure stored-token precedence versus DESIGN and auth-login TTY prompting in JSON/programmatic calls; secure persistence/keyring/no-fallback/profile/RPC evidence must support the actual promised lifecycle |Core implementation independently accepted with39 affected auth tests; final coherent post-trial suite remains pending |
| CLI/package outputs |Built executable human/JSON/stderr/exit and persistent RPC, same recursive declarations and dependency injection; shipped docs match command tree |Final integration audit required; prior312 suite is historical |
| Local proof |Only reviewed bounded ReadOnly packaged-CLI/profile/keyring inventory, CI refusal, no token/URL/argv override; separate from fixtures/default tests |Prior21ReadOnlyPASS; final wrapper changes need offline review before root live run |
| Authoring |AR118 includes all final18 and derived download source/tests/support/proof/setup; exact final100 baseline, same-capability samples and no metric gaming |Pending independent authoring PASS and root closure; reopen if download code arrives later |
| V2 transfer |Publish reviewed follow-up owning all163P2/P3 rows plus supplemental v2 candidates; link it in Issue6 and reconcile counts/remote body |PASS: Issue7 published/exact163-row readback and Issue6 backlinkC44B verified; onlytransfer checked |
| Privacy and release evidence |Full tracked tree plus staged diff privacy gate before any commit; no secrets/private service outputs; linked closing commit or PR and CI evidence |Not satisfied by local tests; no commit/push/merge currently authorized |
| Formal closure |All Issue acceptance satisfied or explicitly transferred, current docs/test proof, linkedv2+commit/PR+CI, ledger close-out verdict |Keep Issue6 OPEN and workstream ACTIVE until these gates hold |

A functional v1 acceptance report may state118/118 plus download1/1 and AR118 PASS when proven,
while formal delivery remains pending release evidence. Do not silently waive an auth/design gap,
mark denied live rows PASS, fabricate a CI run, or close the Issue merely because all wrappers exist.
Routine internal review/verification needs no additional user approval. Commit/push/merge/publication
and closure stay governed by their explicit authorization and evidence gates.

Sources: published Issue6 deterministic acceptance; docs/practices/github-issues.md closure contract;
existing auth-foundation review and workstream history; final source review must verify the actual
Core behavior before resolving those historical caveats. No source changes or live probes in this audit.

## Crosscutting evidence map — static review, not final acceptance

| Open Issue functional item | Existing evidence surface | Final action still required |
|---|---|---|
| Shared factory leaves/help |YouTrack cli.ts and domain command arrays; final two --direct selectors map116 REST leaves to118 endpoints, plus one download leaf |Lead final compiled tree/docs check; do not inflate endpoint count with leaf count |
| Native-fetch operation contracts |Domain *.test.ts and *-cli.test.ts through MSW/native fetch; final catalog/article/download reviews separately owned |Independent final18/download acceptance and coherent final build |
| Permissions and isolation |Every family has gate loops; issue-query ReadOnly POSTs, mutation no-retry and multi-profile RPC tests |Final upload/download/Core regression review; no live mutation needed |
| Full auth lifecycle |YouTrack cli.test.ts validates token-before-storage and unavailable-keyring failure; Core auth-contract/profile-configure/auth tests |Core author/reviewer must close new candidate/prompt/persistence policy; current pass counts not inferred |
| Pagination/projections |reads, query, context, relation, directory, metadata tests cover finite defaults, sparse fields, oversized/empty pages, cursor envelopes and pending count |Final families inherit or prove matching source contracts; no automatic followups |
| CLI/process/output |Real declaration human/JSON/RPC tests, built proof child executable and existing Core/TeamCity process evidence |Final compiled help/package/proof check; no new unsupported AppData override |
| Local proof |profile-proof.test.ts now describes24 fixed rows with7 safety scenarios; wrapper excludes writes/binarydownload and refuses CI/overrides |Reviewer safety verdict and root final run on frozen build; prior21livePASS remains historical |
| Final tests/current docs |Final per-owner suites underway; README already describes final endpoint families |Final npm test, affected TeamCity regression for Core changes, and docs corrections below |

Documentation corrections completed by lead; final frozen audit remains pending:
- Replace README's historical stored-token/TTY auth caveat paragraph after accepted Core correction;
  explain current candidate/prompt and fail-closed persistence behavior/reconfiguration guidance.
- Add the download's exclusive hard-link publication, existing link/junction rejection and explicit
  same-OS-user ancestor-swap limitation from the published contract to durable user documentation.
- Explain the final117 service leaves versus118 REST operations plus the derived leaf; qualify the
  earlier98-leaf/100-operation statement as historical milestone context.

Independent technical reviewer owns final-source correctness; this static audit did not rerun tests,
inspect any live profile or duplicate download/Core security review. No additional material code gap
was established from the existing crosscutting evidence; final functional/release gates remain open.
